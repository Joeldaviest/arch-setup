# Changes require a restart to take effect.

# Install other terminals via Install > Terminal
export TERMINAL=xdg-terminal-exec

# Use VSCodium as the graphical and Git editor
export EDITOR=codium
export VISUAL=codium
export GIT_EDITOR="codium --wait"

# Use a custom directory for screenshots (remember to make the directory!)
# export ARCH_SETUP_SCREENSHOT_DIR="$HOME/Pictures/Screenshots"

# Use a custom directory for screenrecordings (remember to make the directory!)
# export ARCH_SETUP_SCREENRECORD_DIR="$HOME/Videos/Screencasts"
