#!/bin/bash
set -euo pipefail
uri=${1:-mailto:}
address=${uri#mailto:}
address=${address%%\?*}
query="${uri#*\?}"
[[ $query == "$uri" ]] && query=""
exec webapp-launch --url "https://mail.google.com/mail/?view=cm&fs=1&to=$address&$query"
