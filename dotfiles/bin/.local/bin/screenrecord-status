#!/bin/bash
if pgrep -f '^gpu-screen-recorder' >/dev/null; then
  printf '%s\n' '{"text":"󰑋","tooltip":"Recording — click to stop","class":"recording"}'
else
  printf '%s\n' '{"text":"","tooltip":""}'
fi

